type: pkg

project:
  name: wlib-timer
  version: 1.0.0
  keywords:
  - wio
  - pkg
  - embedded
  - timer
  compile_options:
    wio_version: 0.4.2
    default_target: tests

targets:
  tests:
    src: tests
    platform: avr
    framework: cosa
    board: uno
  native-tests:
    src: native-tests
    platform: native

